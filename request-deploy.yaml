apiVersion: apps/v1
kind: Deployment
metadata:
  name: requestmgr-deployment
  namespace: test-ahyeha
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: requestmgr
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate  
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: requestmgr
      namespace: test-ahyeha   
    spec:
      containers:   
      - env:
        - name: JAVA_OPTS
          value: -Dspring.profiles.active=prod,api-docs,no-liquibase
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              key: password
              name: mysql-trq-pass
        - name: SPRING_DATASOURCE_URL
          value: jdbc:mysql://trq:$(MYSQL_PASSWORD)@mysql-read/requestmanager_db?useUnicode=true&characterEncoding=utf8&useLegacyDatetimeCode=false&serverTimezone=UTC&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false      
        image: jed.ocir.io/axulzqeiw39c/request-manager:test
        imagePullPolicy: IfNotPresent
        name: requestmgr
        ports:
        - containerPort: 5056
          protocol: TCP
        resources: {}
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst  
      imagePullSecrets:
      - name: ocirsecrettest
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: requestmgrs-service
  namespace: test-ahyeha
spec:
  type: ClusterIP
  ports:
  - port: 80
    protocol: TCP
    targetPort: 5056
  selector:
    app: requestmgr

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: requestmgrs-ingress-path
  namespace: test-ahyeha
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  tls:
    - hosts:
        - "test.ahyeha.com"
      secretName: apps-tls
  rules:
    - host: "test.ahyeha.com"
      http:
        paths:
          - path: /request-managers(/|$)(.*)
            pathType: Prefix
            backend:
              service:
                name: requestmgrs-service
                port:
                  number: 80
